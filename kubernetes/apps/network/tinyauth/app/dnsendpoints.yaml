---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/externaldns.k8s.io/dnsendpoint_v1alpha1.json
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: tinyauth
  annotations:
    dns.scope: all
spec:
  endpoints:
    - dnsName: auth.riki.boo
      recordType: CNAME
      targets:
        - public.riki.boo
    - dnsName: auth.brauni.eu
      recordType: CNAME
      targets:
        - public.brauni.eu
