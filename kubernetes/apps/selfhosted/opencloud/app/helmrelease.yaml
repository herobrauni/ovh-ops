---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: opencloud
spec:
  chartRef:
    kind: OCIRepository
    name: opencloud
  interval: 30m
  driftDetection:
    mode: enabled
  values:
    controllers:
      opencloud:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            runAsNonRoot: true
        initContainers:
          init-config:
            image:
              repository: docker.io/opencloudeu/opencloud-rolling
              tag: 7.1.0@sha256:0bb29038affb1b58655bb8d4fd3dc78b26fb7c0c7e994bda72e6e4e58e6e54b3
            command:
              - /bin/sh
              - -c
              - |
                if [ -f /etc/opencloud/opencloud.yaml ]; then
                  echo "Config exists, checking for upgrades..."
                  rm -f /etc/opencloud/opencloud.config.patch
                  if opencloud init --diff --insecure=true 2>&1 | grep -q "no changes"; then
                    echo "No config changes needed."
                  else
                    echo "Applying config patch..."
                    patch --no-backup-if-mismatch /etc/opencloud/opencloud.yaml /etc/opencloud/opencloud.config.patch
                  fi
                else
                  echo "No config found, initializing..."
                  opencloud init --insecure=true
                fi
            envFrom:
              - secretRef:
                  name: opencloud-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
        containers:
          opencloud:
            image:
              repository: docker.io/opencloudeu/opencloud-rolling
              tag: 7.1.0@sha256:0bb29038affb1b58655bb8d4fd3dc78b26fb7c0c7e994bda72e6e4e58e6e54b3
            command:
              - opencloud
              - server
            env:
              IDM_CREATE_DEMO_USERS: false
              IDM_ADMIN_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: opencloud-secret
                    key: idm-admin-password
              OC_ENABLE_OCM: false
              OC_INSECURE: false
              OC_LOG_LEVEL: info
              OC_URL: https://opencloud.brauni.dev
              PROXY_TLS: false
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                memory: 1024Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
          radicale:
            image:
              repository: docker.io/opencloudeu/radicale
              tag: latest@sha256:50ac714912e85a36b9cf96e3b246b305447532357ad4b36f4f18cd278f2f3be9
            securityContext:
              allowPrivilegeEscalation: false
              runAsNonRoot: true
              runAsUser: 1000
              runAsGroup: 1000
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    service:
      app:
        controller: opencloud
        ports:
          http:
            port: 9200
    persistence:
      data:
        existingClaim: opencloud
        globalMounts:
          - path: /var/lib/opencloud
            subPath: data
          - path: /etc/opencloud
            subPath: config
      config:
        type: configMap
        identifier: config
        advancedMounts:
          opencloud:
            opencloud:
              - path: /etc/opencloud/proxy.yaml
                subPath: proxy.yaml
            radicale:
              - path: /etc/radicale/config
                subPath: radicale.config
      radicale:
        existingClaim: opencloud
        advancedMounts:
          opencloud:
            radicale:
              - path: /var/lib/radicale
                subPath: radicale
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /tmp
            subPath: tmp
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Selfhosted
          gethomepage.dev/name: OpenCloud
          gethomepage.dev/pod-selector: app.kubernetes.io/name=opencloud
          gethomepage.dev/icon: https://opencloud.brauni.dev/themes/opencloud/assets/logo.svg
          gethomepage.dev/href: https://opencloud.brauni.dev
        hostnames:
          - files.brauni.dev
          - opencloud.brauni.dev
        parentRefs:
          - name: envoy-brauni-dev-public
            namespace: network
            sectionName: https
    configMaps:
      config:
        data:
          proxy.yaml: |-
            additional_policies:
              - name: default
                routes:
                  - endpoint: /caldav/
                    backend: "http://127.0.0.1:5232"
                    remote_user_header: X-Remote-User
                    skip_x_access_token: true
                    additional_headers:
                      - X-Script-Name: /caldav
                  - endpoint: /.well-known/caldav
                    backend: "http://127.0.0.1:5232"
                    remote_user_header: X-Remote-User
                    skip_x_access_token: true
                    additional_headers:
                      - X-Script-Name: /caldav
                  - endpoint: /carddav/
                    backend: "http://127.0.0.1:5232"
                    remote_user_header: X-Remote-User
                    skip_x_access_token: true
                    additional_headers:
                      - X-Script-Name: /carddav
                  - endpoint: /.well-known/carddav
                    backend: "http://127.0.0.1:5232"
                    remote_user_header: X-Remote-User
                    skip_x_access_token: true
                    additional_headers:
                      - X-Script-Name: /carddav
          radicale.config: |-
            [server]
            hosts = 0.0.0.0:5232

            [auth]
            type = http_x_remote_user

            [storage]
            filesystem_folder = /var/lib/radicale/collections
            predefined_collections = {
              "def-addressbook": {
                "D:displayname": "Personal Address Book",
                "tag": "VADDRESSBOOK"
              },
              "def-calendar": {
                "C:supported-calendar-component-set": "VEVENT,VJOURNAL,VTODO",
                "D:displayname": "Personal Calendar",
                "tag": "VCALENDAR"
              }
              }
