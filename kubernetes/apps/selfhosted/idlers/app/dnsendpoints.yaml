---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/externaldns.k8s.io/dnsendpoint_v1alpha1.json
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: idlers
  annotations:
    dns.scope: all
spec:
  endpoints:
    - dnsName: idlers.brauni.dev
      recordType: CNAME
      targets:
        - public.brauni.dev
