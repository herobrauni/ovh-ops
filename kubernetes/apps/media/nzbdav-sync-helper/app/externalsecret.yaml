---
# yaml-language-server: $schema=https://kubernetes-schemas.brauni.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nzbdav-sync-helper
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden
  target:
    name: nzbdav-sync-helper-secret
    creationPolicy: Owner
    template:
      data:
        PLEX_TOKEN: "{{ .PLEX_TOKEN }}"
        NZBDAV_API_KEY: "{{ .NZBDAV_API_KEY }}"
  dataFrom:
    - extract:
        key: sync-helper
