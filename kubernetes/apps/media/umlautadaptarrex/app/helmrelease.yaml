---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app umlautadaptarrex
spec:
  chartRef:
    kind: OCIRepository
    name: umlautadaptarrex
  interval: 1h
  driftDetection:
    mode: enabled
  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile: { type: RuntimeDefault }
    controllers:
      umlautadaptarrex:
        containers:
          app:
            image:
              repository: lexfi/umlautadaptarrex
              tag: 1.0.0@sha256:12c6039b3f78c6512cfe7929cf3d4c7cb27130741cec8a31d53bfb1f00432744
            env:
              TZ: Europe/Berlin
              PUID: "1000"
              PGID: "1000"
              LOG_LEVEL: info
              PORT: &apiPort 5005
              WEB_PORT: &webPort 5007
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/health
                    port: *apiPort
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/health
                    port: *apiPort
                  failureThreshold: 30
                  periodSeconds: 10
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    service:
      app:
        controller: umlautadaptarrex
        ports:
          http:
            port: *webPort
          api:
            port: *apiPort
          proxy:
            port: 5006
    persistence:
      data:
        existingClaim: umlautadaptarrex
        globalMounts:
          - path: /data
      tmp:
        type: emptyDir
