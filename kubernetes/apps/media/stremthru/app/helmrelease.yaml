---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app stremthru
spec:
  chartRef:
    kind: OCIRepository
    name: stremthru
  interval: 1h
  driftDetection:
    mode: enabled
  values:
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile: { type: RuntimeDefault }
    controllers:
      stremthru:
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
          secret.reloader.stakater.com/reload: stremthru-secret,stremthru-initdb-secret,stremthru-pguser-secret
        initContainers:
          01-init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.3@sha256:6fa1f331cddd2eb0b6afa7b8d3685c864127a81ab01c3d9400bc3ff5263a51cf
            envFrom:
              - secretRef:
                  name: stremthru-initdb-secret
        containers:
          app:
            image:
              repository: docker.io/muniftanjim/stremthru
              tag: 0.98.9@sha256:d40cdf8e59943ed7f1411c2744b59e1023233cb545e1c634fdcfe813f9925b80
            env:
              STREMTHRU_BASE_URL: https://stremthru.480p.com
              STREMTHRU_PORT: &targetPort 8080
              STREMTHRU_DATA_DIR: /app/data
              STREMTHRU_DATABASE_URI:
                valueFrom:
                  secretKeyRef:
                    name: &pguser stremthru-pguser-secret
                    key: uri
              STREMTHRU_REDIS_URI: redis://stremthru-dragonfly.media.svc.cluster.local:6379
            envFrom:
              - secretRef:
                  name: stremthru-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop:
                  - ALL
    service:
      app:
        controller: stremthru
        ports:
          http:
            port: 80
            targetPort: *targetPort
    persistence:
      config:
        existingClaim: stremthru
        globalMounts:
          - path: /app/data
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
