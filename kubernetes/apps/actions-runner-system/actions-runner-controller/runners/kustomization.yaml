---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  # - ./nix
  # schemas-runner removed with .github/workflows/schemas.yaml — nothing runs on it anymore
