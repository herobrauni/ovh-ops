---
# yaml-language-server: $schema=https://kubernetes-schemas.brauni.dev/cdi.kubevirt.io/storageprofile_v1beta1.json
apiVersion: cdi.kubevirt.io/v1beta1
kind: StorageProfile
metadata:
  name: ceph-block-kubevirt
spec:
  claimPropertySets:
    - accessModes:
        - ReadWriteMany
      volumeMode: Block
