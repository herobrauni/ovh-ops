---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: flux-operator
  annotations:
    external-dns.custom/type: technitium
spec:
  endpoints:
    - dnsName: flux-operator.riki.boo
      recordType: CNAME
      targets:
        - secure.riki.boo
