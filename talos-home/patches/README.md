# Patches are applied to the generated machine configs by talhelper,
# mirroring ../talos/patches/ conventions.
#
# global/           — applied to all nodes (incl. volume layout, KubeSpan, ingress firewall)
# controller/       — applied to the (combined) control-plane nodes
