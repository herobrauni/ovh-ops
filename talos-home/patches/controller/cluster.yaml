# Home variant of talos/patches/controller/cluster.yaml
cluster:
  # Combined nodes: these control planes are also the only workers.
  allowSchedulingOnControlPlanes: true
  apiServer:
    admissionControl:
      $$patch: delete

    extraArgs:
      # https://kubernetes.io/docs/tasks/extend-kubernetes/configure-aggregation-layer/
      enable-aggregator-routing: true
      feature-gates: MutatingAdmissionPolicy=true
      runtime-config: admissionregistration.k8s.io/v1beta1=true

  controllerManager:
    extraArgs:
      bind-address: 0.0.0.0
  # NOTE: coreDNS stays Talos-managed here (the OVH cluster disables it and runs
  # its own under kubernetes/apps/kube-system/coredns — not needed at home).
  etcd:
    extraArgs:
      listen-metrics-urls: http://0.0.0.0:2381
    advertisedSubnets:
      - 10.100.15.0/24
  proxy:
    disabled: true
  scheduler:
    extraArgs:
      bind-address: 0.0.0.0
